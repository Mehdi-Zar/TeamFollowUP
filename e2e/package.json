{
  "name": "teamfollowup-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests driving the built application in a browser.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
